{{#each songs}}
	<div>
		<div>
			<h3>{{title}}</h3>
			{{!-- {{album}} {{artist}} --}}
			{{album}} {{artist}}
		</div>
		<div>
			<input type="button" id="{{@key}}" class="edit-button" value="Edit"><input type="button" id="{{@key}}" class="delete-button" value="Delete">
		</div>
	</div>
{{/each}}


{{!-- {{#each songs}}
	<div>
		<h3>{{title}}</h3>
			<div class="row">
				<div class=".col-md-1">{{artist}}</div>
				<div class=".col-md-1 .col-md-offset-1">{{album}}</div>
			</div>
		<input type="button" id="{{@key}}" class="edit-button" value="Edit">
		<input type="button" id="{{@key}}" class="delete-button" value="Delete">
	</div>
{{/each}} --}}
